import React from 'react'; 
import Redirect from 'react-router-dom';
import Container, Table, SearchInput ) from "/styles";

tied slanges

 const Justificaciones ( Justificaciones,

auth,

justificar, deleteJustificacion

if (auth.uid) return Redirect to/admin"

return

Header Container

<HeaderContainer

chi>Relación de justificaciones</a> HeaderContainer
(data to 
    <SearchInput

placeholders Busqueda por dni" Atno "tern"

// onthange-tesetTera(e.target.value))